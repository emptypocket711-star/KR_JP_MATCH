{
  "name": "hana-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules tests for Hana",
  "scripts": {
    "test": "JAVA_HOME=\"${JAVA_HOME:-/Applications/Android Studio.app/Contents/jbr/Contents/Home}\" firebase emulators:exec --project demo-hana-rules --config ../../firebase.json --only firestore,storage \"jest --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "jest": "^30.2.0"
  }
}
